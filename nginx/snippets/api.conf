# Timeout for reading from the upstream server.
proxy_read_timeout 60s;

# Timeout for sending request to the upstream server.
proxy_send_timeout 60s;

# Cache only safe HTTP methods.
# Prevent caching of POST/PUT/DELETE requests.
proxy_cache_methods GET HEAD;
